---
title: "Timer"
description: "Countdown timer with pause/resume and auto-start"
---

<ComponentPreview base="ink" name="timer-demo" hideCode />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @termcn/ink/timer
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install ink
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  src="registry/bases/ink/ui/types.ts"
  title="components/ui/types.ts"
/>

<ComponentSource
  src="registry/bases/ink/themes/default.ts"
  title="lib/terminal-themes/default.ts"
/>

<ComponentSource
  base="ink"
  name="theme-provider"
  title="components/ui/theme-provider.tsx"
/>

<ComponentSource
  src="registry/bases/ink/hooks/use-input.ts"
  title="hooks/use-input.ts"
/>

<ComponentSource
  src="registry/bases/ink/hooks/use-interval.ts"
  title="hooks/use-interval.ts"
/>

<ComponentSource base="ink" name="timer" title="components/ui/timer.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { Timer } from "@/components/ui/timer";
```

```tsx
<Timer duration={300} format="ms" label="Pomodoro" autoStart />
```

## API Reference

### Timer

| Prop         | Type                   | Default    |
| ------------ | ---------------------- | ---------- |
| `duration`   | `number`               | `required` |
| `onComplete` | `() => void`           | -          |
| `autoStart`  | `boolean`              | `false`    |
| `format`     | `"hms" \| "ms" \| "s"` | `"hms"`    |
| `color`      | `string`               | -          |
| `label`      | `string`               | -          |
