---
title: "List"
description: "Navigable list with optional filtering"
---

<ComponentPreview base="ink" name="list-demo" hideCode />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @termcn/ink/list
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install ink
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  src="registry/bases/ink/ui/types.ts"
  title="components/ui/types.ts"
/>

<ComponentSource
  src="registry/bases/ink/themes/default.ts"
  title="lib/terminal-themes/default.ts"
/>

<ComponentSource
  base="ink"
  name="theme-provider"
  title="components/ui/theme-provider.tsx"
/>

<ComponentSource
  src="registry/bases/ink/hooks/use-input.ts"
  title="hooks/use-input.ts"
/>

<ComponentSource base="ink" name="list" title="components/ui/list.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { List } from "@/components/ui/list";
```

```tsx
<List
  items={[
    { key: "ts", label: "TypeScript", description: "Typed JavaScript" },
    { key: "rs", label: "Rust", description: "Systems language" },
    { key: "go", label: "Go", description: "Cloud-native language" },
  ]}
  onSelect={(item) => console.log(item.key)}
/>
```

## API Reference

### List

| Prop         | Type                       | Default    |
| ------------ | -------------------------- | ---------- |
| `items`      | `ListItem[]`               | `required` |
| `onSelect`   | `(item: ListItem) => void` | -          |
| `filterable` | `boolean`                  | `false`    |
| `height`     | `number`                   | `10`       |
| `cursor`     | `string`                   | `"›"`      |

### ListItem

| Prop          | Type     | Default    |
| ------------- | -------- | ---------- |
| `key`         | `string` | `required` |
| `label`       | `string` | `required` |
| `description` | `string` | -          |
| `color`       | `string` | -          |
