---
title: "Input"
description: "Text input components for capturing user input in the terminal."
---

OpenTUI keyboard events are global, so inputs are inactive by default. Use
`autoFocus` for a single input, or control `focused` when rendering multiple
inputs. Set `isDisabled` to ignore keyboard and paste events.

<ComponentsList category="input" />
