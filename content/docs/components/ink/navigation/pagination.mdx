---
title: "Pagination"
description: "Page navigation with prev/next and jump"
---

<ComponentPreview base="ink" name="pagination-demo" hideCode />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @termcn/ink/pagination
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install ink
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  src="registry/bases/ink/ui/types.ts"
  title="components/ui/types.ts"
/>

<ComponentSource
  src="registry/bases/ink/themes/default.ts"
  title="lib/terminal-themes/default.ts"
/>

<ComponentSource
  base="ink"
  name="theme-provider"
  title="components/ui/theme-provider.tsx"
/>

<ComponentSource
  base="ink"
  name="pagination"
  title="components/ui/pagination.tsx"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { Pagination } from "@/components/ui/pagination";
```

```tsx
<Pagination total={10} current={page} onChange={(p) => setPage(p)} />
```

## API Reference

### Pagination

| Prop        | Type                     | Default    |
| ----------- | ------------------------ | ---------- |
| `total`     | `number`                 | `required` |
| `current`   | `number`                 | `required` |
| `onChange`  | `(page: number) => void` | -          |
| `showEdges` | `boolean`                | `true`     |
| `siblings`  | `number`                 | `1`        |
