---
title: "Breadcrumb"
description: "Breadcrumb navigation trail"
---

<ComponentPreview base="ink" name="breadcrumb-demo" hideCode />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @termcn/ink/breadcrumb
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install ink
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  src="registry/bases/ink/ui/types.ts"
  title="components/ui/types.ts"
/>

<ComponentSource
  src="registry/bases/ink/themes/default.ts"
  title="lib/terminal-themes/default.ts"
/>

<ComponentSource
  base="ink"
  name="theme-provider"
  title="components/ui/theme-provider.tsx"
/>

<ComponentSource
  src="registry/bases/ink/hooks/use-input.ts"
  title="hooks/use-input.ts"
/>

<ComponentSource
  base="ink"
  name="breadcrumb"
  title="components/ui/breadcrumb.tsx"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { Breadcrumb } from "@/components/ui/breadcrumb";
```

```tsx
<Breadcrumb
  items={[
    { key: "home", label: "Home", onSelect: () => {} },
    { key: "docs", label: "Docs" },
  ]}
  activeKey="docs"
/>
```

## API Reference

### Breadcrumb

| Prop        | Type               | Default    |
| ----------- | ------------------ | ---------- |
| `items`     | `BreadcrumbItem[]` | `required` |
| `separator` | `string`           | `"›"`      |
| `activeKey` | `string`           | -          |

### BreadcrumbItem

| Prop       | Type         | Default    |
| ---------- | ------------ | ---------- |
| `label`    | `string`     | `required` |
| `key`      | `string`     | `required` |
| `onSelect` | `() => void` | -          |
