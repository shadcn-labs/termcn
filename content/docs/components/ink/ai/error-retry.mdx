---
title: "Error Retry"
description: "Inline error callout with retry and dismiss shortcuts"
---

<ComponentPreview base="ink" name="error-retry-demo" hideCode />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @termcn/ink/error-retry
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install ink
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  src="registry/bases/ink/ui/types.ts"
  title="components/ui/types.ts"
/>

<ComponentSource
  src="registry/bases/ink/themes/default.ts"
  title="lib/terminal-themes/default.ts"
/>

<ComponentSource
  base="ink"
  name="theme-provider"
  title="components/ui/theme-provider.tsx"
/>

<ComponentSource
  base="ink"
  name="error-retry"
  title="components/ui/error-retry.tsx"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { ErrorRetry } from "@/components/ui/error-retry";
```

```tsx
<ErrorRetry
  error={new Error("Network error")}
  retryCount={0}
  maxRetries={3}
  onRetry={() => refetch()}
  onDismiss={() => setError(null)}
  isActive
/>
```

## Shortcuts

When `retryCount < maxRetries` and `isActive` is true:

- **Enter** or **r** — calls `onRetry`
- **Esc** — calls `onDismiss`

After `retryCount >= maxRetries`, only **Esc** dismisses; retry shortcuts are disabled.

## API Reference

### ErrorRetry

| Prop         | Type              | Default    |
| ------------ | ----------------- | ---------- |
| `error`      | `Error \| string` | `required` |
| `onRetry`    | `() => void`      | -          |
| `onDismiss`  | `() => void`      | -          |
| `retryCount` | `number`          | `0`        |
| `maxRetries` | `number`          | `3`        |
| `isActive`   | `boolean`         | `true`     |
