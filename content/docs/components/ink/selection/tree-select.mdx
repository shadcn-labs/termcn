---
title: "Tree Select"
description: "Hierarchical tree selection"
---

<ComponentPreview base="ink" name="tree-select-demo" hideCode />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @termcn/ink/tree-select
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install ink
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  src="registry/bases/ink/ui/types.ts"
  title="components/ui/types.ts"
/>

<ComponentSource
  src="registry/bases/ink/themes/default.ts"
  title="lib/terminal-themes/default.ts"
/>

<ComponentSource
  base="ink"
  name="theme-provider"
  title="components/ui/theme-provider.tsx"
/>

<ComponentSource
  src="registry/bases/ink/hooks/use-input.ts"
  title="hooks/use-input.ts"
/>

<ComponentSource
  base="ink"
  name="tree-select"
  title="components/ui/tree-select.tsx"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { TreeSelect } from "@/components/ui/tree-select";
```

```tsx
<TreeSelect
  label="Select a file"
  nodes={[
    {
      value: "src",
      label: "src",
      children: [
        { value: "index.ts", label: "index.ts" },
        { value: "utils.ts", label: "utils.ts" },
      ],
    },
    { value: "README.md", label: "README.md" },
  ]}
  onChange={(value) => {}}
/>
```

## API Reference

### TreeSelect

| Prop                | Type                  | Default    |
| ------------------- | --------------------- | ---------- |
| `nodes`             | `TreeSelectNode<T>[]` | `required` |
| `value`             | `T`                   | -          |
| `onChange`          | `(value: T) => void`  | -          |
| `onSubmit`          | `(value: T) => void`  | -          |
| `label`             | `string`              | -          |
| `expandedByDefault` | `boolean`             | `false`    |
